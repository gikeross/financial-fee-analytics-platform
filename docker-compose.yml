services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: fee_analytics
      POSTGRES_USER: analytics
      POSTGRES_PASSWORD: analytics
    ports: ["5432:5432"]
    volumes: [fee_analytics_pg:/var/lib/postgresql/data]
volumes:
  fee_analytics_pg:
